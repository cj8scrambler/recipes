# Recipes Application Environment Variables
# Copy this file to .env and customize for your environment

# ========================================
# CHOOSE YOUR DATABASE SETUP
# ========================================
# 
# OPTION 1: Local MySQL Container (recommended for development/testing)
# ---------------------------------------------------------------------
# Leave DATABASE_URL commented out and configure the MYSQL_* variables below.
# A MySQL container will be created automatically when you run:
#   docker-compose --profile internal-db up -d
#
# OPTION 2: External Database (recommended for production)
# ---------------------------------------------------------
# Set DATABASE_URL to your external database connection string.
# Comment out or remove the MYSQL_* variables.
# Then run:
#   docker-compose up -d
#

# ========================================
# Database Configuration - OPTION 1: Local Container
# ========================================
# These variables are used when running with internal MySQL container
# Uncomment and configure these for local container setup:

MYSQL_ROOT_PASSWORD=rootpassword
MYSQL_DATABASE=recipes
MYSQL_USER=recipes_user
MYSQL_PASSWORD=recipes_password
MYSQL_PORT=3306

# ========================================
# Database Configuration - OPTION 2: External Database
# ========================================
# Uncomment and set DATABASE_URL for external database.
# When this is set, the MYSQL_* variables above are ignored.
# Format: mysql+pymysql://username:password@hostname:port/database
# Example:
# DATABASE_URL=mysql+pymysql://recipes_user:mypassword@db.example.com:3306/recipes

# ========================================
# Backend Configuration
# ========================================
BACKEND_PORT=8000
FLASK_ENV=production
SECRET_KEY=change-this-to-a-random-secret-key-in-production

# CORS Origins - comma-separated list of allowed frontend origins
# For Docker: use http://localhost:5173 (default)
# For production: add your domain, e.g., https://recipes.example.com
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# ========================================
# Frontend Configuration
# ========================================
FRONTEND_PORT=5173
VITE_API_URL=http://localhost:8000
